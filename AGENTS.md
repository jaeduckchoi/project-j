# 종구의 식당 · 프로젝트 작업 지침

이 저장소는 `projectJ.uproject`를 루트로 하는 Unreal Engine 5.8 / Paper2D 프로젝트다. 예전 Unity 프로젝트의 경로·생성 절차를 현재 작업에 적용하지 않는다. 사용자가 이번 작업에서 정한 범위와 변경 요청을 우선 반영한다.

## 시작할 때

- [문서 안내](Docs/README.md)에서 작업에 필요한 현재 문서만 읽는다. 게임 규칙은 [GAMEPLAY.md](Docs/Design/GAMEPLAY.md), UI는 [UI.md](Docs/Design/UI.md), 제작은 [AUTHORING.md](Docs/Development/AUTHORING.md)를 기준으로 한다.
- 소스·저장된 애셋의 구현과 문서가 다르면 불일치를 먼저 구분한다. 과거 보고서나 리서치 제안을 현재 승인된 기능으로 간주하지 않는다.
- 작업 전 기존 변경을 확인하고 보존한다. 파일·텍스트 검색은 `rg`를 우선 사용한다. 개인 `.codex`·IDE 설정과 사용자 저장을 일반 정리 대상으로 다루지 않는다.

## 구현과 원본

- 게임 실행은 저장된 Blueprint로 구성한다. Python은 `Content/Python/jonggu`의 에디터 제작·검증 패키지다. 완료 기준은 별도 요청이 없다면 Unreal 에디터 Play다.
- 생성되는 동작의 지속적인 수정은 해당 Python 정본에서 한다. 애셋만 직접 수정하면 다음 Build에서 덮어써질 수 있다. 소스·에셋 경로와 명명 규칙은 [STRUCTURE.md](Docs/Development/STRUCTURE.md)를 따른다.
- 프로젝트 경로는 `jonggu.paths`, 애셋 경로는 `jonggu.assets`, 기본 배치는 `SourceData/Restaurant/layout.json`에서 관리한다. Hub 귀환의 현재 고정 좌표 예외는 제작 안내에 명시되어 있다. 현재 PC의 드라이브 경로를 새 코드·문서에 고정하지 않는다. 문서의 로컬 링크는 해당 문서 기준 상대 경로로 쓰고, 명령과 축약 파일 경로에는 기준 디렉터리를 명시한다.
- 기본 식당 Build는 기존 플레이어·아트·충돌·전체 월드 임포트를 실행하지 않는다. 관련 작업이 요청 범위에 포함될 때만 `Tools/World`의 정본과 검증 절차를 사용한다. 외부 Unity·이전 마이그레이션 자료는 참고 자료다.
- 맵의 배치된 Pawn·`SourceCamera`, 원본 ID·소유권 태그를 보존한다. Actor 표시 이름과 게임 식별자를 혼용하지 않는다.
- `.uasset`·`.umap`의 이동·이름 변경은 Unreal API로 처리하고 참조·컴파일·맵 재열기를 검증한다. 미사용 파일은 참조와 생성 입력 여부를 확인한 뒤 정리하고, 필요한 원본은 `Saved` 밖에 둔다.

## 저장과 UI 계약

- 슬롯 `JongguPrototypeCheckpoint`, 스키마 버전 1, `BP_PrototypeSave`의 직렬화 클래스 이름과 이전 패키지 리다이렉트는 현재 저장 호환 계약이다. 관련 변경은 변경 전 저장 fixture의 실제 로드로 검증한다. 사용자 슬롯을 테스트에 사용하지 않는다.
- GameInstance는 맵을 넘는 상태를, 맵별 관리자는 주문·조리·접시·표시 데이터를 관리한다. HUD 접기 상태는 세션에만 남긴다. UI는 게임 판정을 소유하지 않는다.
- 표시·클릭·가림 영역은 공통 정의를 사용한다. 팝업의 이동 차단과 전체 일시정지를 구분한다. 기존 E 표시와 기구 위 조리 진행 말풍선도 별개다.

## 검증과 기록

- [제작·검증 안내](Docs/Development/AUTHORING.md)에 따라 변경에 필요한 검사만 실행한다. 별도 QA 에디터를 여는 런처는 현재 프로젝트 에디터의 작업을 저장하고 정상 종료한 뒤 실행한다.
- 코드·애셋 변경은 관련 소스 검사, Blueprint 컴파일, 실제 Play를 확인한다. UI 변경은 세 기준 해상도에서 확인한다. 문서만 수정했다면 링크·경로·내용 일관성을 검사하며 게임 재생성은 필요하지 않다.
- 기존 충돌 미통과 항목은 [BASELINE.md](Docs/Validation/BASELINE.md)와 비교한다. 자동 검사, 실제 렌더 확인, 수동 조작감·재미 평가는 구분한다. 실행 시각·범위·실패·미실행을 기록하며 과거 통과 결과를 이번 실행처럼 쓰지 않는다.
- 게임 규칙·구조·명령이 바뀌면 해당 현재 문서를 함께 갱신한다. 과거 결과는 `Docs/Archive`에 날짜와 현재 문서 링크를 남긴다. 문서 역할과 보관 규칙은 [문서 안내](Docs/README.md)를 따른다.
- 결과는 한국어로 변경 내용·검증·남은 한계를 간결하게 설명한다. 커밋할 때는 `타입: 한국어 내용` 한 줄을 쓰고 마침표를 붙이지 않는다. 타입은 `feat`, `update`, `fix`, `docs`, `design`, `style`, `rename`, `delete`, `refactor`, `test`, `chore` 중 하나다.
