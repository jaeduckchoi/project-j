# 첫 프로토타입 구현·검증 결과

검증 시각: 2026-09-14T00:50:26+09:00. 대상: `D:/laeti-dev/ProjectJ/projectJ.uproject`, Unreal Engine 5.8.2의 실제 Play In Editor. 독립 실행 파일은 만들지 않았습니다.

## 구현 결과

준비 → 선택적 해변 채집 → 1~2종 메뉴와 4/6명 손님 선택 → 자동 조리·직접 수령·서빙 → 결산 → 다음 날 준비를 저장된 Blueprint로 구현했습니다. 조개 없이 기본 영업을 마칠 수 있고, 조개 1개로 만든 특선 두 그릇은 일반 찌개 주문에 제공됩니다.

팬·냄비 점유, 접시 한 개 운반, 잘못된 서빙 보존, 반납·완성품 비우기, 유입 중단·재개, 조기 마감, 대기 보너스, 전체 일시정지, 구간 저장·재시도를 포함합니다. 메뉴·조리 선택창과 상태·주문·기구 진행률·결산 UI를 마우스로 조작합니다. Esc/F10은 일시정지, Shift+Esc는 프로젝트의 에디터 Play 종료입니다.

## 통과한 기능 검사

| 검사 묶음 | 결과 | 실제 확인한 범위 |
|---|---:|---|
| 구간 저장 Blueprint | 19항목 통과 | 초기 저장, 채집 중복 방지, 재고·매출·메뉴 일괄 복원, 날짜 한 번 전환, 누락·다른 자료형·지원하지 않는 버전의 저장 거부 |
| 영업 규칙 Blueprint | 46항목 통과 | 탐험 없는 4명 영업, 메뉴 제약, 특선 2그릇, 접시·폐기, 기구 점유, 유입·마감, 추가 보상 경계값, 재시도 |
| 실제 PIE 순환 | 31항목 통과 | 두 맵 왕복, 3개 채집, 같은 날 재생 방지, 특선 직접 수령·서빙, 결산·다음 날, 실제 맵 재로드 재시도, 새 Play에서 Hub·Beach 저장 복원 |
| 월드 구성 | 통과 | 두 맵 저장·재열기, 새 Blueprint 컴파일, SourceCamera·기존 Pawn 사용, 원본 Hub 115개·Beach 130개 Actor 보존 |
| 화면 확인 | 통과 | 준비·메뉴·해변·조리·영업·결산·일시정지 캡처에서 한글, 비용·수량, 진행률과 표시 위치 확인 |

기능 검사는 모델로 흉내 낸 별도 규칙이 아니라 컴파일된 게임 Blueprint 함수를 실제 PIE에서 호출했습니다. 영업 규칙 비교에는 런타임 Tick이 사용하는 `Advance`를 호출했고, 전체 일시정지는 실제 엔진 시간이 멈춘 상태를 별도로 확인했습니다. 이동은 원래 Pawn에 입력을 주어 UI 종료·맵 이동 후 실제 위치 변화와 메뉴 중 정지를 확인했습니다.

마우스 메뉴 검사는 입력 처리 후 전달되는 동일 클릭 좌표 경로를 호출했습니다. 운영체제의 물리 키·마우스 입력을 직접 누르는 검사는 하지 않았습니다. 창의 검은 여백을 제외한 클릭 좌표 변환은 16:9·가로/세로 여백 등 14개 좌표 사례로 별도 검사했습니다.

모든 실행 검사는 UUID가 붙은 검증 전용 저장 슬롯을 사용하고 끝난 뒤 지웠습니다. 사용자용 `JongguPrototypeCheckpoint`는 검증으로 덮어쓰지 않았습니다. 최종 PIE 로그에는 `Accessed None`, Blueprint 런타임 오류 또는 Python 예외가 없습니다.

원본 `BP_JongguPlayer`와 `BP_JongguGameMode` 파일은 작업 전 SHA256과 동일합니다. 신규 코드는 원본 플레이어 생성기·전체 마이그레이션을 실행하지 않습니다.

## 기존 검증과 구분할 사항

작업 전에 기존 `left_then_up_only_corner` 해변 시나리오를 다시 실행했습니다. 594개 검사 중 초기 방향·위 방향·옆 미끄러짐을 확인하는 3개가 미통과였습니다. 이전 기록도 같은 시나리오의 옆 미끄러짐 항목이 미통과였습니다. 새 재현의 첫 이동 표본이 0.4초여서 0.18초 방향 전환보다 늦습니다. 이 결과를 이번 식당 기능의 새 회귀나 원본 이동 전체 통과로 보고하지 않습니다.

원본과 신규 범위의 구분은 [기준 기록](D:/laeti-dev/ProjectJ/Tools/Prototype/BASELINE.md), 실제 재현은 [baseline_collision.json](D:/laeti-dev/ProjectJ/Saved/PrototypeQA/baseline_collision.json)에 있습니다. 원본 가림·카메라·충돌 구성은 보존했으며, 모든 가림 경계를 사람이 다시 걸어 확인한 것은 아닙니다.

## 구현 구조에서 구체화한 선택

재사용 상호작용 계약은 별도 Unreal BPI 에셋 대신 공통 `BP_PrototypeInteractable`의 `interaction_id`와 `GetInteractionId` 함수로 제공합니다. 근접 탐색과 배치의 정본은 `layout.json`이며 변경 후 전용 구성 도구로 반영합니다. 요리 정의·주문·완성품 자료형은 명시적인 Blueprint 데이터 객체로 분리했습니다.

HUD는 Blueprint Canvas이며 창 크기에 맞추어 표시됩니다. GameInstance가 날짜·재고·메뉴·누적 매출을 관리하고, 각 맵의 관리자는 진행 중 주문·조리·접시를 관리합니다. Python은 에디터 제작·검증에만 사용합니다.

## 재미에 관한 관찰

실제 Blueprint 생산 비교에서 팬 공유 메뉴 두 그릇은 20초, 팬·냄비 분산 메뉴는 16초에 준비됐습니다. 기본 찌개 두 그릇은 조리 2회·32초, 특선 두 그릇은 조리 1회·16초·조개 1개였습니다. 이는 수령·폐기 시간을 0으로 둔 생산 측정이며 이동·서빙·재미 평가가 아닙니다.

사람의 메뉴 선택 이유, 조개를 찾은 동기, 주문량을 조절한 시점과 이유는 아직 관찰하지 않았습니다. 조건 순서와 기록 양식은 [플레이 관찰 안내](D:/laeti-dev/ProjectJ/Tools/Prototype/PLAYTEST.md)에 남겼습니다. 이번에 선택하지 않은 보고서의 제안은 후속 가설로 유지합니다.

상세 실행 결과: [build_report.json](D:/laeti-dev/ProjectJ/Saved/PrototypeQA/build_report.json), [pie_report.json](D:/laeti-dev/ProjectJ/Saved/PrototypeQA/pie_report.json). 화면 파일은 `Saved/PrototypeQA/Screenshots`에 있습니다.

## 2026-09-14 팝업 디자인 추가 검증

Unity Hub의 베이지 픽셀 프레임과 Galmuri11을 모든 팝업에 적용했습니다. Blueprint/저장 19개와 실제 Play 120개 검사를 통과했습니다. 1280×720, 1920×1080, 1189×862에서 각각 11개 상태를 실제 PIE PNG로 기록하고 정확한 크기를 검사했습니다. 상세 구현·검증 방법·화면은 [POPUP_DESIGN.md](D:/laeti-dev/ProjectJ/Tools/Prototype/POPUP_DESIGN.md)를 참고하세요. 기존 이동·충돌 미통과 항목과 사람의 재미 평가는 별도 기록을 유지합니다.

## 2026-09-14 메인 HUD 추가 검증

Unity Hub의 갈색 픽셀 패널·코인·안내 상자를 일반 HUD에도 적용했습니다. 구간 저장 19개, 기존 실제 Play 120개, 새 HUD 실제 Blueprint 7개 검사를 통과했습니다. 1280×720, 1920×1080, 1189×862에서 준비·영업·특선·유입 중단·마감·해변·팝업 등 총 24개 PNG를 확인했습니다. 캐릭터를 가리던 접시·조개 패널을 오른쪽으로 옮기고, 조리 중에는 완료 문구 대신 남은 시간을 표시합니다. 표시용 전달과 일시정지 버튼 연결 외 게임 규칙·저장 형식은 유지합니다. 상세 결과와 화면은 [MAIN_HUD_DESIGN.md](D:/laeti-dev/ProjectJ/Tools/Prototype/MAIN_HUD_DESIGN.md)에 있습니다.


## 2026-09-14 요약 HUD·자동 투명화·조리 말풍선 추가 검증

주문·기구·소지품의 독립 접기, 같은 Play 내 상태 유지, 플레이어 전체 Bounds에 따른 패널별 투명화, 팬·냄비 위 조리 진행·완료 수량 말풍선을 적용했습니다. 기존 E 표시와 게임 규칙·저장 형식을 유지합니다. 일반 HUD 패널 면적은 두 말풍선과 중앙 안내를 포함해 33.0% 줄었습니다.

Blueprint 컴파일과 기능 검사 164개를 통과했습니다. 세 해상도 1280×720, 1920×1080, 1189×862의 실제 PIE 원본 PNG 103장으로 요약·상세·혼합, 동시 조리·특선 수량, 모든 10개 그룹의 가림·복원, 호버·클릭·경계·검은 여백을 검사했습니다. 운영체제 키보드·마우스를 직접 사용하는 수동 조작감 평가는 수행하지 않았습니다. 기존 충돌 기준의 미통과 항목은 별도로 유지합니다. 캡처의 시간 고정과 별도 실시간 검증 범위는 [ADAPTIVE_HUD.md](D:/laeti-dev/ProjectJ/Tools/Prototype/ADAPTIVE_HUD.md), 상세 수치는 [최종 보고서](D:/laeti-dev/ProjectJ/Saved/AdaptiveHUDQA/final_report.json)에 기록했습니다.
