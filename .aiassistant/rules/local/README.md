# 로컬 규칙 메모

이 폴더는 개인별, 머신별 메모를 두는 공간이다.
공유 규칙의 정본은 아니다.

기준:

- 공유 저장소에는 이 `README.md`만 유지한다.
- 개인 실행 경로, Unity 설치 위치, 배치 명령, 임시 체크리스트는 로컬에만 둔다.
- 로컬 메모가 팀 규칙이 되면 `rules/project/*` 또는 관련 도메인 문서로 승격하고, 인덱스와 엔트리 파일 링크를 함께 갱신한다.
- 개인 계정 정보, 토큰, 절대 경로는 공유 문서로 올리지 않는다.